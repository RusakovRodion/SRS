/**
 * Общая информация по работе с API:
 *  - Каждый метод должен возвращать данные, либо кидать исключение
 *  - Обработка исключения - ответственность вызывающего
 *  - В случаях когда метод возвращает массив допустимо вернуть пустой массив, вместо кидания исключения
 *  - Если метод не возвращает полезных данных, то ничего возвращать не нужно (Promise<void>)
 *  - Все методы - async функции, возвращают Promise<T>
 *  - Интерфейсы аргументов и возвращаемых значений должны быть описаны
 */
